Pro preparedirsn, dir, version, outdir=outdir
  zfile = file_search(dir+'v'+str(version)+'/*v'+str(version)+'*Epoch00*F850LP_drz.fits')
  if zfile eq '' then return
  preparefile, zfile[0], outdir=outdir
end
